<!DOCTYPE html>
<html lang="en">
  <%- include("header") -%>
  <body>
    <!-- <% if (listTitle == "Saturday" || listTitle == "Sunday") { %>
    <h1 style="color: red"><%=listTitle%>'s To Do List</h1>
    <% } else { %>
    <h1 style="color: rgb(16, 173, 16)"><%=listTitle%>'s To Do List</h1>
    <% } %> -->

    <div class="box" id="heading">
      <h1><%= listTitle %></h1>
    </div>

    <div class="box">
      <% for(var i=0; i<newListItems.length; i++){ %>
        <form method="post" action="/delete">
        <div class="item">
          <input type="checkbox" name="checkbox" value="<%=newListItems[i]._id %>" onchange="this.form.submit()">
          <p><%= newListItems[i].name %></p>
        </div>
        <input type="hidden"  name="listName" value="<%= listTitle %>"></input>
      </form>
      <% } %>

      <% newListItems.forEach(function(item){
        
      })  %>

      <form class="item" method="post" action="/">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off" />
        <button type="submit" name="list" value=<%= listTitle %> >+</button>
      </form>

    </div>
  </body>
  <%- include("footer") -%>
</html>
